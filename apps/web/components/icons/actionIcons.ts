/**
 * Curated action icon set, re-exported from lucide-react (MIT licensed —
 * https://lucide.dev) under names that match DosyaLab's product vocabulary.
 * Consumers should import from here rather than reaching into lucide-react
 * directly, so the icon-to-action mapping stays centralized and consistent.
 */
export {
  Merge as MergeIcon,
  Split as SplitIcon,
  Shrink as CompressIcon,
  ScanText as OcrIcon,
  Lock as LockIcon,
  Unlock as UnlockIcon,
  Download as DownloadIcon,
  Upload as UploadIcon,
  Settings as SettingsIcon,
} from 'lucide-react';
